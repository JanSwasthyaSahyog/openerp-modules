<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data noupdate="1">

        <!--Email template-->
        <record id="email_template_auto_customer" model="email.template">
            <field name="name">Send auto email</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">New Reorder Levels Set ${object.name}</field>
            <field name="email_to">${object.company_id and object.company_id.email or 'ajeenckya@satvix.com'}</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.lang}</field>
            <field name="body_html"><![CDATA[
                """ 
                <p>Reorder Levels Updated. Please check on link </p>
                """            
            ]]></field>
        </record>
  
  </data>
</openerp>